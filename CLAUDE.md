# UPSTREAM SOURCE OF TRUTH

This repo is a public, unlisted GitHub Pages MIRROR of a research overview. The canonical upstream (source folder, build script, memory-bank) auto-loads via the operator's local ~/.claude registry; it is deliberately not committed here because this repo is public.
